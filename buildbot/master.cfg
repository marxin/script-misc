# -*- python -*-
# ex: set syntax=python:

import os

# This is a sample buildmaster config file. It must be installed as
# 'master.cfg' in your buildmaster's base directory.

# This is the dictionary that the buildmaster pays attention to. We also use
# a shorter alias to save typing.
c = BuildmasterConfig = {}

####### BUILDSLAVES

# The 'slaves' list defines the set of recognized buildslaves. Each element is
# a BuildSlave object, specifying a unique slave name and password.  The same
# slave name and password must be configured on the slave.
from buildbot.buildslave import BuildSlave
c['slaves'] = [
	BuildSlave("marxinbox-slave", "pass", max_builds = 1),
	BuildSlave('marxinbook-slave', 'pass', max_builds = 1),
	BuildSlave('evans-slave', '22d71061dfcda672b35a05c909e83bb8', max_builds = 1)]

# 'slavePortnum' defines the TCP port to listen on for connections from slaves.
# This must match the value configured into the buildslaves (with their
# --master option)
c['slavePortnum'] = 9989

####### CHANGESOURCES

# the 'change_source' setting tells the buildmaster how it should find out
# about source code changes.  Here we point to the buildbot clone of pyflakes.

from buildbot.changes.gitpoller import GitPoller
c['change_source'] = []
c['change_source'].append(GitPoller(
	'https://github.com/marxin/gcc.git',
        workdir='gcc-git',
	branches = True,
        pollinterval=20))

c['change_source'].append(GitPoller(
	'https://github.com/marxin/llvm-project.git',
        workdir='llvm-git',
	branches = True,
        pollinterval=20))


####### SCHEDULERS

# Configure the Schedulers, which decide how to react to incoming changes.  In this
# case, just kick off a 'runtests' build

from buildbot.schedulers.basic import SingleBranchScheduler
from buildbot.schedulers.basic import AnyBranchScheduler
from buildbot.schedulers.forcesched import ForceScheduler
from buildbot.changes import filter
from buildbot.changes.filter import ChangeFilter


c['schedulers'] = []
c['schedulers'].append(AnyBranchScheduler(
                            name="all",
			    change_filter = ChangeFilter(branch_re = '^buildbot.*'),
			    fileIsImportant = lambda change: change.asDict()['comments'].startswith('buildbot'),
                            treeStableTimer=None,
                            builderNames=["gcc-spec2006", 'llvm-spec2006']))

####### BUILDERS

# The 'builders' list defines the Builders, which tell Buildbot how to perform a build:
# what steps, and which slaves can execute them.  Note that any particular build will
# only take place on one slave.

from buildbot.process.factory import BuildFactory
from buildbot.steps.source.git import Git
from buildbot.steps.shell import ShellCommand
from buildbot.steps.slave import MakeDirectory
from buildbot.steps.shell import Compile
from buildbot.process.properties import WithProperties
from buildbot.steps.transfer import FileUpload
from buildbot.steps import shell

# CREATE GCC factory that compiles GCC from git repository
def create_gcc_factory():
  gcc_build_dir = 'build/builddir'
  f = BuildFactory()

  f.addStep(Git(repourl='git@github.com:marxin/gcc.git', mode='incremental', haltOnFailure = True))
  f.addStep(shell.SetPropertyFromCommand(command="cat /proc/cpuinfo | grep processor | wc -l", property="nproc", haltOnFailure = True))
  f.addStep(ShellCommand(command=['rm', '-rf', 'builddir'], haltOnFailure = True))
  f.addStep(MakeDirectory(gcc_build_dir, haltOnFailure = True))
  f.addStep(ShellCommand(command=['./../configure', '--disable-bootstrap', '--enable-languages=c,c++,fortran', '--disable-libsanitizer', '--disable-multilib', '--enable-checking=release', WithProperties('--prefix=%(builddir)s/install/gcc-%(revision)s')], workdir = gcc_build_dir, haltOnFailure = True))
  f.addStep(Compile(command = ['make', WithProperties('-j%(nproc)s')], workdir = gcc_build_dir, haltOnFailure = True))
  f.addStep(Compile(command = ['make', 'install'], workdir = gcc_build_dir, haltOnFailure = True))

  return f

# CREATE LLVM factory that compiles LLVM from git repository
def create_llvm_factory():
  llvm_build_dir = 'build/llvm/builddir'
  f = BuildFactory()

  default_path = {'PATH': '/abuild/mliska/bin/gcc492/bin:${PATH}', 'LD_LIBRARY_PATH': '/abuild/mliska/bin/gcc492/lib64:${LD_LIBRARY_PATH}'}

  f.addStep(Git(repourl='git@github.com:marxin/llvm-project.git', mode='incremental', haltOnFailure = True))
  f.addStep(shell.SetPropertyFromCommand(command="cat /proc/cpuinfo | grep processor | wc -l", property="nproc", haltOnFailure = True))
  f.addStep(ShellCommand(command=['rm', '-rf', 'llvm/builddir'], haltOnFailure = True))
  f.addStep(ShellCommand(command=['ln', '-s', '../../clang', 'clang'], workdir = 'build/llvm/tools'))
  f.addStep(MakeDirectory(llvm_build_dir, haltOnFailure = True))
  f.addStep(ShellCommand(command=['./../configure', '--enable-optimized', WithProperties('--prefix=%(builddir)s/install/llvm-%(revision)s')], workdir = llvm_build_dir, haltOnFailure = True, env = default_path))
  # add GCC 4.9.2 to PATH for evans machine
  f.addStep(Compile(command = ['make', WithProperties('-j%(nproc)s')], env = default_path, workdir = llvm_build_dir, haltOnFailure = True))
  f.addStep(Compile(command = ['make', 'install'], env = default_path, workdir = llvm_build_dir, haltOnFailure = True))

  return f

# APPEND spec step to an existing factory with given compiler_prefix
def append_spec_benchmark(f, compiler_prefix):
  f.addStep(
	  ShellCommand(
		  command = [
			  '../../source/script-misc/spec_buildbot.py',
			  '../../source/cpu2006',
			  WithProperties(compiler_prefix + '-%(revision)s'),
			  WithProperties('/tmp/spec-' + compiler_prefix + '-%(revision)s.json'),
			  compiler_prefix],
			  env = { 'PATH': WithProperties('%(builddir)s/install/' + compiler_prefix + '-%(revision)s/bin:${PATH}') }
		  , haltOnFailure = True
	  ))
  f.addStep(FileUpload(slavesrc = WithProperties('/tmp/spec-' + compiler_prefix + '-%(revision)s.json'), masterdest = WithProperties('reports/spec-' + compiler_prefix + '-%(revision)s.json'), haltOnFailure = True))

# GCC RUN
gcc_spec_factory = create_gcc_factory()
append_spec_benchmark(gcc_spec_factory, 'gcc')

# LLVM RUN
llvm_spec_factory = create_llvm_factory()
append_spec_benchmark(llvm_spec_factory, 'llvm')

from buildbot.config import BuilderConfig

c['builders'] = []
c['builders'].append(
    BuilderConfig(name="gcc-spec2006",
      slavenames=['evans-slave', 'marxinbook-slave'],
      factory=gcc_spec_factory))

c['builders'].append(
    BuilderConfig(name="llvm-spec2006",
      slavenames=['evans-slave', 'marxinbook-slave'],
      factory=llvm_spec_factory))

####### STATUS TARGETS

# 'status' is a list of Status Targets. The results of each build will be
# pushed to these targets. buildbot/status/*.py has a variety to choose from,
# including web pages, email senders, and IRC bots.

c['status'] = []

from buildbot.status import html
from buildbot.status.web import authz, auth

authz_cfg=authz.Authz(
    # change any of these to True to enable; see the manual for more
    # options
    auth=auth.BasicAuth([("pyflakes","pyflakes")]),
    gracefulShutdown = False,
    forceBuild = 'auth', # use this to test your slave once it is set up
    forceAllBuilds = False,
    pingBuilder = False,
    stopBuild = False,
    stopAllBuilds = False,
    cancelPendingBuild = False,
)
c['status'].append(html.WebStatus(http_port=8010, authz=authz_cfg))

####### PROJECT IDENTITY

# the 'title' string will appear at the top of this buildbot
# installation's html.WebStatus home page (linked to the
# 'titleURL') and is embedded in the title of the waterfall HTML page.

c['title'] = "SPEC tester"
c['titleURL'] = "http://suse.com"

# the 'buildbotURL' string should point to the location where the buildbot's
# internal web server (usually the html.WebStatus page) is visible. This
# typically uses the port number set in the Waterfall 'status' entry, but
# with an externally-visible host name which the buildbot cannot figure out
# without some help.

c['buildbotURL'] = "http://localhost:8010/"

####### DB URL

c['db'] = {
    # This specifies what database buildbot uses to store its state.  You can leave
    # this at its default for all but the largest installations.
    'db_url' : "sqlite:///state.sqlite",
}
